import { imagesFormat, imagesFormatToWidth } from './format';
import { imagesGenerate } from './generate';

export const images = {
  format: imagesFormat,
  formatToWidth: imagesFormatToWidth,
  generate: imagesGenerate,
};
